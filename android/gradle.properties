org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Trỏ Gradle dùng đúng JDK (tránh lỗi "supplied javaHome seems to be invalid")
org.gradle.java.home=E:\\_Compliers\\JavaJDK\\JavaJDK21

# Tránh lỗi "this and base files have different roots" khi project (E:) và pub cache (C:) khác ổ đĩa:
# Kotlin incremental compiler không tính được path tương đối giữa hai ổ.
kotlin.incremental=false
