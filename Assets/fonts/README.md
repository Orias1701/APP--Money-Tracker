# Font San Francisco (SF Pro Text)

App dùng font **SF Pro Text** của Apple toàn cục.

- Tải bộ font SF Pro tại: https://developer.apple.com/fonts/
- Giải nén và đặt file **SFProText-Regular.ttf** vào thư mục này (`assets/fonts/`).

Sau đó chạy lại `flutter pub get` và build app.
